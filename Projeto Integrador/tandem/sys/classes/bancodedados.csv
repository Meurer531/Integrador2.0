CREATE DATABASE tandem

CREATE TABLE USUARIO(
    id_usuario SERIAL PRIMARY KEY,
    loginUsuario VARCHAR(255),
    senha VARCHAR(255),
    email VARCHAR(255),
    telefone BIGINT(8),
    celular BIGINT(9),
    tipo ENUM('cliente', 'vendedor')
);